package ahhenderson.core.managers.dependency.facadeService.controller
{
	

	/**
	 * Create and register <code>Proxy</code>s with the <code>Model</code>.
	 */
	public class AmfServiceCommand 
	{
 

	/*	override public function execute(note:INotification):void
		{

			const serviceRequest:AmfServiceRequest = note.getBody() as AmfServiceRequest
				
			if(!serviceRequest)
				throw new Error(FacadeErrorConstants.SERVICE_REQUEST_GROUP_REQUEST_DOES_NOT_EXIST);
			
			if(!(this.facade as KFacade))
				throw new Error(FacadeErrorConstants.FACADE_IS_NOT_OF_TYPE_KFACADE);
			 
			// Execute single service call.
			FacadeServiceManager.instance.invokeAMFServiceCall.apply(null, 
				[(this.facade as KFacade).facadeId, 
				serviceRequest.serviceDefinition].concat(serviceRequest.args));

		 
		}*/
	}
}
